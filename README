Giteway


        /\Deployment instructions/\


Giteway is a web app. It requires a JDK 7 and Maven 3.

First, clone the repository in your local environment. 

----------------------------------JETTY----------------------------------
To start the web app with the jetty plugin, run the following command from the app root:

mvn jetty:run [options]

Options:
-Dhttp.proxyHost=<arg>
-Dhttp.proxyPort=<arg>


----------------------------------WAR----------------------------------
To build the war, run the following command from the app root:
mvn package